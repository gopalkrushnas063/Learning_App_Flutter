import 'package:flutter/material.dart';



class AllCourseList extends StatelessWidget {
  const AllCourseList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("All Courses")),
      ),
    );
  }
}